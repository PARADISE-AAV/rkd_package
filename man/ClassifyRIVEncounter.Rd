% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ClassifyRIVEncounter.R
\name{ClassifyRIVEncounter}
\alias{ClassifyRIVEncounter}
\title{This function use the different CPD algorithms to classify the Encounter of RIV data}
\usage{
ClassifyRIVEncounter(
  RKDdata,
  output_path,
  algorithm,
  interval_from_diagnostics = 6,
  CM_data = NULL,
  IV_data = NULL,
  nb_month = 24
)
}
\arguments{
\item{RKDdata}{RIV data from \code{\link{Merge_Encounter_initial}} function}

\item{output_path}{folder where the Redcap data will be saved}

\item{algorithm}{function use to classify the RIV patient, the possibility are "Paradise_Encounter" or "CPD Relapse" or "Treatment On/Off" or "CPD LTROT" or "CPD ANCA" or "CPD Treatment" or "CPD LTROT current"}

\item{interval_from_diagnostics}{the interval from diagnostics for the algorithm Paradise_Encounter by default 6}

\item{CM_data}{Data from \code{\link{CPD_Medication_Treatment}} function for the Treatment On/Off algorithm}

\item{IV_data}{Data from \code{\link{CPD_IVTherapy_Treatment}} function for the Treatment On/Off algorithm}

\item{nb_month}{Number of month out of treatment for the CPD LTROT function}
}
\value{
The Redcap data with the classification variables in your folder and in an R object
}
\description{
The objective is to classify the patient of RIV data based on criteria described in the function apply
}
\details{
This function allows you to call one of  multiple functions to create a classification variable for each RKD encounter. The possible functions each create one new variable
\itemize{
\item \code{\link{CPDRelapse}} tells us if an encounter is in relapse or not based on rules and models
\item \code{\link{Paradise_Encounter}} tells us if we can include the Encounter or not in the Paradise project. To be use after \code{\link{CPDRelapse}}
\item \code{\link{CPD_Treatment_OnOff}} tells us if an Encounter is under treatment or not
\item \code{\link{CPD_LTROT}} tells us if a patient is in Long Term Remission Out of Treatment (LTROT)
\item \code{\link{CPD_ANCA}} tells us the switch of ANCA from one Encounter to the other.
\item \code{\link{CPD_Treatment}} tells us which treatment is used at each encounter. To be use after \code{\link{CPD_Treatment_OnOff}}
\item \code{\link{CPD_LTROT_current}} tells us if an Encounter is a LTROT
}
}
\author{
Matthieu COQ
}
